import { PlaceholderPage } from "@/components/site/placeholder-page";
import { placeholderPages } from "@/lib/site-pages";

export default function BlogPage() {
  return <PlaceholderPage page={placeholderPages.blog} />;
}
